ARS Electron SIP Client — это настольное приложение (софтфон) для совершения голосовых звонков через Интернет (VoIP). Приложение построено на базе фреймворка Electron и использует библиотеку SIP.js для сигнализации и WebRTC для передачи аудио.Приложение разработано для работы с корпоративными АТС (Asterisk, FreeSWITCH) по протоколу WSS (Secure WebSocket).🚀 ВозможностиSIP-регистрация: Подключение к АТС через защищенный WebSocket (WSS).Голосовые звонки: Совершение исходящих и прием входящих вызовов.Всплывающие уведомления: Отдельное окно (Popup) при входящем звонке.Обход сетевых ограничений: Встроенные механизмы подмены DNS (Host Mapping) для работы в сложных сетевых конфигурациях.Сохранение настроек: Автоматическое запоминание учетных данных пользователя.🛠️ Технологический стекElectron: Основа приложения (Chromium + Node.js).SIP.js: JavaScript библиотека для работы с SIP-протоколом.WebRTC: Протокол для передачи потокового аудио в реальном времени.Electron-Store: Для локального хранения настроек.📥 Установка и ЗапускДля работы приложения вам понадобится установленный Node.js.1. Клонирование репозиторияСкачайте проект на свой компьютер:Bashgit clone https://github.com/Zgen332/ARS-Electron-app.git
cd ARS-Electron-app
2. Установка зависимостейЭта команда скачает все необходимые библиотеки (включая сам Electron):Bashnpm install
3. Запуск приложенияДля запуска в режиме разработки выполните:Bashnpm start
⚙️ Настройка подключенияПосле запуска приложения откроется окно настроек. Для корректной работы необходимо ввести данные вашей SIP-учетной записи.Пример конфигурации:ПараметрОписаниеПример значенияSIP Сервер (Domain)Адрес вашей АТС (Realm)f2.ads365.ruПортПорт для WSS подключений6060ЛогинВаш внутренний номер505ПарольПароль от SIP-аккаунтаВаш_ПарольВажно: Приложение автоматически применяет сетевые правила (DNS Spoofing) для перенаправления доменного имени на рабочий IP-адрес сервера. Это настроено в файле main.js.🧠 Как это работает (Архитектура)Приложение состоит из двух основных процессов:Main Process (main.js):Управляет жизненным циклом приложения (создание окон).Отвечает за сетевые хаки: принудительно направляет доменные запросы на нужный IP, игнорирует ошибки SSL-сертификатов (если они самоподписанные).Обрабатывает IPC-сообщения (связь между окнами).Renderer Process (src/js/sip-manager.js, app.js):SIPManager: Класс-обертка над библиотекой sip.js. Он инициализирует UserAgent, регистрируется на сервере и управляет сессиями звонков.Интерфейс: HTML/CSS отрисовывают панель управления и реагируют на действия пользователя.Схема звонка:Приложение устанавливает WSS-соединение с сервером.Отправляет SIP-запрос REGISTER.При звонке устанавливается P2P-соединение через WebRTC для передачи голоса.
