Конечно\! Вот этот же текст, оформленный в чистом Markdown, с использованием заголовков, списков, кода и эмодзи для лучшей читаемости.

-----

# 📞 ARS Electron SIP Client

**ARS Electron SIP Client** — это настольное приложение (софтфон) для совершения голосовых звонков через Интернет (VoIP). Приложение построено на базе фреймворка **Electron** и использует библиотеку **SIP.js** для сигнализации и **WebRTC** для передачи аудио.

Приложение разработано для работы с корпоративными АТС (Asterisk, FreeSWITCH) по протоколу **WSS (Secure WebSocket)**.

-----

## 🌟 Возможности и функции

  * **SIP-регистрация:** Подключение к АТС через защищенный WebSocket (WSS).
  * **Голосовые звонки:** Совершение исходящих и прием входящих вызовов.
  * **Управление звонком:** Кнопки "Принять", "Завершить", "Удержать/Снять с удержания".
  * **Регулировка громкости:** Управление громкостью звука от собеседника.
  * **Обход сетевых ограничений:** Встроенные механизмы подмены DNS (Host Mapping) для работы в сложных сетевых конфигурациях.
  * **Сохранение настроек:** Автоматическое запоминание учетных данных пользователя.

-----

## 🛠️ Технологический стек

| Технология | Назначение |
| :--- | :--- |
| **Electron** | Основа приложения (Desktop App Framework). |
| **Node.js** | Среда выполнения. |
| **SIP.js** | Библиотека для работы с SIP-протоколом. |
| **WebRTC** | Протокол для передачи потокового аудио в реальном времени. |
| **Electron-Store** | Для локального хранения настроек. |

-----

## ⬇️ Установка и Запуск

Для работы приложения вам понадобится установленный **Node.js**.

### 1\. Клонирование репозитория

```bash
git clone https://github.com/Zgen332/SIP-client.git
cd SIP-client
```

### 2\. Установка зависимостей

Эта команда скачает все необходимые библиотеки (включая сам `electron.exe`):

```bash
npm install
```

### 3\. Запуск приложения

Для запуска в режиме разработки выполните:

```bash
npm start
```

-----

## ⚙️ Настройка подключения

После запуска приложения откроется окно настроек. Введите данные вашей SIP-учетной записи и нажмите **"Сохранить и Подключиться"**.

| Параметр | Описание | Пример значения |
| :--- | :--- | :--- |
| **SIP Сервер (Domain)** | Адрес вашей АТС (Realm) | `domain` |
| **Порт** | Порт для WSS подключений | `port` |
| **Логин** | Ваш внутренний номер | `100` |
| **Пароль** | Пароль от SIP-аккаунта | `password` |

> **Примечание:** В файле `main.js` включены специальные сетевые правила (например, Host Mapping) для обхода потенциальных проблем с DNS и SSL-сертификатами, что делает приложение более надежным в локальных сетях.

-----

## 🧠 Принцип работы (Архитектура)

Приложение использует стандартную двухпроцессную архитектуру Electron:

1.  **Main Process (`main.js`):**

      * Управляет окнами и жизненным циклом приложения.
      * Применяет **сетевые хаки** (Host Resolver Rules) перед запуском браузера.

2.  **Renderer Process (`src/js/app.js`, `sip-manager.js`):**

      * Содержит весь пользовательский интерфейс и логику.
      * Класс **`SIPManager`** инициирует транспорт (WSS) и управляет звонками с помощью библиотеки **SIP.js**.
      * После успешного соединения аудио-поток передается напрямую по **WebRTC**.
