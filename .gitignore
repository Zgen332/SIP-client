# 🛑 КРИТИЧЕСКИ ВАЖНЫЕ ПАПКИ (Зависимости и исполняемые файлы)
# Папка с установленными модулями Node.js
/node_modules

# Папки, создаваемые сборщиками (Electron-builder, Electron-packager)
/dist
/out
/build

# 📂 Временные и сгенерированные файлы
npm-debug.log
yarn-error.log
.DS_Store # Файлы macOS
*.bak # Резервные копии

# 🔒 Electron-specific файлы
# Файлы, которые могут содержать локальные настройки или кэш Electron
.electron-flags.json 
.electron-version
.vscode/ # Если вы используете VS Code, и там есть специфические локальные настройки

# ⚙️ Electron Store (Локальные данные пользователя)
# Этот файл содержит сохраненные настройки SIP (логин, пароль)
# Если вы не хотите, чтобы ваши личные настройки попали в репозиторий, добавьте его.
# Имя файла зависит от конфигурации в main.js, но обычно это:
config.json